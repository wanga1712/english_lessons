# English Lessons App

Приложение для автоматической обработки видеоуроков английского языка и создания интерактивных упражнений для детей 7 лет.

## Описание

Приложение автоматически:
1. Мониторит папку с видеоуроками
2. Транскрибирует видео в текст (локально через Whisper)
3. Анализирует контент через OpenRouter AI
4. Создаёт структурированные уроки с карточками упражнений

## Требования

- Python 3.11+
- PostgreSQL
- FFmpeg (для извлечения аудио из видео)
- OpenRouter API ключ

## Установка

### 1. Клонирование и настройка окружения

```bash
# Активация виртуального окружения
.\venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Установка FFmpeg

**Windows:**
- Скачайте FFmpeg с https://ffmpeg.org/download.html
- Добавьте в PATH или укажите полный путь в настройках

### 3. Настройка базы данных PostgreSQL

Создайте базу данных:
```sql
CREATE DATABASE english_lessons;
CREATE USER your_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE english_lessons TO your_user;
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта (скопируйте из `.env.example`):

```env
# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL Database
DB_NAME=english_lessons
DB_USER=your_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# OpenRouter AI
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_MODEL=openai/gpt-4o-mini

# Video Processing
WATCHED_VIDEO_DIRECTORY=D:/english_lessons
TEMP_AUDIO_DIRECTORY=./temp_audio

# Whisper Model (base, small, medium, large)
WHISPER_MODEL=base
```

### 5. Применение миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### 6. Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

## Использование

### Запуск мониторинга папки с видео

```bash
# Мониторинг новых файлов
python manage.py watch_videos

# Мониторинг + обработка существующих файлов
python manage.py watch_videos --process-existing
```

### Запуск Django сервера

```bash
python manage.py runserver
```

### API Endpoints

- `GET /api/lessons/` - Список всех уроков
- `GET /api/lessons/<id>/` - Детали урока с карточками
- `GET /api/videos/` - Список всех видеофайлов
- `POST /api/videos/<id>/process/` - Ручная обработка видео

### Админ-панель

Доступна по адресу: `http://localhost:8000/admin/`

## Структура проекта

```
english_lessons/
├── english_lessons/      # Настройки Django проекта
├── lessons/              # Основное приложение
│   ├── models.py        # Модели БД
│   ├── admin.py         # Админ-панель
│   ├── views.py         # API endpoints
│   ├── services/        # Бизнес-логика
│   │   ├── transcription_service.py  # Whisper транскрипция
│   │   ├── openrouter_service.py     # OpenRouter AI
│   │   ├── video_processor.py        # Пайплайн обработки
│   │   └── video_watcher.py          # Мониторинг папки
│   └── management/
│       └── commands/
│           └── watch_videos.py       # Команда мониторинга
├── manage.py
├── requirements.txt
└── .env
```

## Модели данных

### VideoFile
- Информация о видеофайле
- Статусы: pending, processing, done, error

### Lesson
- Урок с темой, описанием, транскриптом
- Связь 1:1 с VideoFile

### ExerciseCard
- Карточки упражнений для урока
- Типы: repeat, translate, choose, color, speak, match

## Процесс обработки

1. **Обнаружение файла** - Watcher обнаруживает новый видеофайл
2. **Транскрипция** - Whisper преобразует видео в текст
3. **Фильтрация** - Очистка текста от артефактов
4. **AI анализ** - OpenRouter определяет тему и создаёт упражнения
5. **Сохранение** - Создание урока и карточек в БД

## Примечания

- Первый запуск Whisper загрузит модель (может занять время)
- Рекомендуется использовать модель `base` для быстрой обработки
- Для лучшего качества используйте `small` или `medium`
- OpenRouter API требует интернет-соединение
- **Автоматическое определение GPU/CPU**: Приложение автоматически определяет доступность GPU (CUDA) и использует его для ускорения транскрипции. Если GPU недоступен или имеет недостаточно памяти (<2GB), используется CPU.
- **Оптимизация производительности**: Код оптимизирован для работы с большими объемами данных:
  - Используются `select_related` и `prefetch_related` для предотвращения N+1 запросов
  - Bulk операции для массового создания карточек
  - Модульная структура кода (не более 200 строк на модуль)

## UI - Игровая карта приключений

Приложение включает современный игровой интерфейс в стиле Uchi.ru:

### Главная страница - Карта приключений
- **URL**: `http://localhost:8000/` (редирект на `/uchi/`)
- Интерактивная карта с островами знаний
- HTML5 Canvas для отрисовки игровой карты
- Последовательные пути между уроками
- Визуальная индикация статуса уроков:
  - 🔒 Заблокированные (серые)
  - 📚 Доступные (золотые)
  - 🎯 Текущий урок (синий с свечением)
  - ✅ Пройденные (зеленые)

### Панель учителя
- **URL**: `http://localhost:8000/teacher/`
- Загрузка видеофайлов (drag & drop)
- Обработка видео
- Мониторинг статуса обработки

### Страница тем урока
- **URL**: `http://localhost:8000/lesson/<id>/uchi/`
- Группировка карточек по темам
- Прогресс по каждой теме
- Интерактивные карточки упражнений

## Разработка

### Архитектура UI
- Модульная структура views (не более 200 строк на модуль)
- Разделение на `views_uchi.py` (игровой UI) и `views_lesson.py` (стандартный UI)
- Canvas-based карта для лучшей производительности
- Адаптивный дизайн для мобильных устройств

### API Endpoints для фронтенда
- `GET /api/lessons/` - Список всех уроков с прогрессом
- `GET /api/lessons/<id>/` - Детали урока
- `GET /api/lessons/<id>/topics/` - Темы урока
- `POST /api/lessons/<id>/start/` - Начать урок
- `POST /api/cards/answer/` - Отправить ответ на карточку

